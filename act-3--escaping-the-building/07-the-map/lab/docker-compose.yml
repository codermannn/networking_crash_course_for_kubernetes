version: '3.8'

services:
  workbench:
    image: nicolaka/netshoot:latest
    container_name: routing_workbench
    privileged: true # The building manager's master key to build a router and configure tables
    stdin_open: true
    tty: true
